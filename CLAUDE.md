# dltmddyd321.github.io — Claude 작업 지침

Astro 기반 개인 기술 블로그. 글 작성 방법 등 일반 안내는 [README.md](./README.md) 참고.

아래 규칙들은 작업하며 사용자와 정한 것으로, **어느 기기에서 작업하든 동일하게 적용된다.**
(저장소에 두는 이유: 로컬 메모리는 기기별로 따로 놀아서 다른 맥북에서 클론하면 사라짐)

## 1. 회사(TimeBlocks) 코드 기밀 유지 — 반드시 지킬 것

TimeBlocks(회사 Android 앱, `android-5.x` 레포) 관련 작업을 소재로 글을 쓸 때, **실제 코드나 로직을 그대로든 살짝 바꿔서든 절대 재현하지 않는다.** 항상 의사코드(pseudocode)로 추상화하거나, 실제 구현이 역추적되지 않도록 이름/구조를 충분히 바꿔서 넣는다.

**왜:** 사용자가 명시적으로 지시함 — "TimeBlocks 관련은 회사 코드기 때문에 중요한 기술 코드가 외부로 절대 유출되어서는 안돼". 이건 스타일 취향이 아니라 기밀 유지 경계선이므로 예외 없이 지킨다.

**적용 방법:**
- 글을 쓰기 전에 다음이 포함되는지 확인: 실제 클래스/함수/변수명, 정확한 비즈니스 로직·알고리즘, 실제 API 엔드포인트나 요청/응답 형태, 회사 레포에서 그대로(부분적으로라도) 가져온 코드 블록
- 구체적인 식별자는 일반화된 가짜 이름으로 교체한다 (예: 실제 `AppTheme`/`NewAppTheme` → `ThemeManager`/`ThemeResolver` 같은 이름, 실제 엔드포인트 → `/api/theme/attributes` 같은 범용 예시)
- 정확한 구현을 재현하기보다 문제의 형태와 디버깅/설계 방법론을 이야기한다 (예: "기기에 캐시된 DB를 직접 조회해서 원인을 증명했다", "로컬 기본값이 없는 이미지 필드가 회전 시 null로 떨어졌다")
- 이 규칙은 어느 프로젝트 세션에서 글을 쓰든 동일하게 적용된다 — 이 레포의 코드/빌드에 국한된 규칙이 아니라, "회사 관련 소재를 다루는 글"이라는 주제 자체에 붙는 규칙이다
- 참고로 사용자는 애초에 회사 내부 로직이나 너무 단순한 이슈로는 글 작성을 잘 요청하지 않을 것으로 예상됨 — 그럼에도 디버깅 후기 등 소재로 다룰 만한 글은 이 정제 과정을 반드시 거쳐야 함

## 2. 작업 완료 = 커밋 + 푸시까지

수정이 끝나고 빌드(`npm run build`)가 통과하면, 따로 물어보지 말고 `main`에 커밋·푸시한다.

**왜:** `main` 푸시 시 GitHub Actions가 배포한다([.github/workflows/deploy.yml](./.github/workflows/deploy.yml)). 로컬에만 있는 변경은 라이브에 없다 — 새로 만든 `/write` 페이지가 404 나서 사용자가 혼란스러워한 적이 있음. 이후 사용자가 "블로그 작업은 무조건 그렇게까지 다 해줘"라고 명시함.

**적용 방법:** 푸시 후 배포 완료까지 확인하고 알린다. 단, 사용자가 아직 검토 중인 미완성 작업이나 "먼저 diff 보여달라"고 한 경우는 예외.

## 3. AI Preview는 모든 글에 — 알아서 채운다

`src/content/posts/*.md`는 전부 frontmatter에 `aiPreview`를 가져야 하고, **Claude가 글을 읽고 직접 작성한다.** 사용자가 `/write`로 쓴 글도 포함된다.

**왜:** 원래 "Claude가 쓴 글에만 붙이는 필드"로 정의했더니, 사용자가 `/write`로 쓴 글(그 폼엔 입력칸이 없음)에 배너가 안 붙었다. 사용자가 범위를 정정함 — "AI 프리뷰는 블로그 게시글을 작성한다고 하면 너가 글을 요약한 내용을 무조건 알아서 등록해줘야해". 요청을 기다리지 말고 알아서 할 것.

**적용 방법:**
- 이 레포에서 작업할 때마다 `aiPreview:` 줄이 없는 글이 있는지 확인하고 채운다. `description`을 바꿔 쓰지 말고 **글을 읽고** 실제 내용을 요약한다.
- 스키마에서는 **선택 항목으로 유지한다.** 필수로 바꾸면 사용자가 `/write`로 발행하는 순간 빌드가 실패해 배포가 막힌다.

## 4. AI Preview 문구에 도입부를 붙이지 않는다

"핵심만 보면 —", "요약하면", "정리하자면" 같은 말머리 없이 **바로 본론으로 시작한다.**

**왜:** 배너에 이미 "✦ AI Preview" 라벨이 있어서 도입부는 라벨을 한 번 더 말하는 셈이고, 모든 글에 같은 문구가 반복되면 템플릿처럼 읽힌다. 사용자 지적 — "AI 프리뷰에 핵심만 보면 — 이거 왜 자꾸 붙여?"

이 블로그 문장 전반에 적용되는 감각이기도 하다. 무슨 이야기를 할지 예고하는 대신 그냥 그 이야기를 한다.

## 5. 디자인은 공용 클래스로 — 컴포넌트마다 베끼지 않는다

반복되는 시각 패턴(예: 아웃라인 pill 버튼)은 `src/styles/global.css`에 **공용 클래스로 한 번만 정의**하고 모두 그걸 쓴다. 컴포넌트마다 비슷한 padding/border/font-size를 따로 들고 있지 않게 한다.

**왜:** 글 페이지의 수정/삭제/공유하기 버튼 높이가 제각각이었는데, 원인은 `<a>` 하나와 `<button>` 둘을 각각 손으로 스타일링했고 `<a>`와 `<button>`의 기본 크기 계산이 달라 padding만으로는 맞춰지지 않았기 때문이다. 사용자 지적 — "이런 블로그라도 대충하지 말고 정확한 디자인 시스템 개념을 갖추어서 구성해야해".

**적용 방법:**
- 새 버튼/뱃지를 만들기 전에 `global.css`에 이미 있는 공용 클래스부터 확인한다.
- 중복 스타일 때문에 생긴 버그는 각각 숫자를 맞추지 말고 **공용 클래스로 통합해서** 고친다.
- 컨트롤류는 padding만으로 크기를 잡지 말고 `height` + `box-sizing: border-box`를 명시한다.

## 6. 이름 표기 — 한글 문장 안에서는 한글로

- **한글(이승용):** 문장 안에 조사(`의`, `는`)나 어미(`입니다`)가 붙는 자리 — 메타 설명, About 페이지 소개 문장 등
- **영문(LeeSeungYong):** 문법적으로 문장의 일부가 아닌 독립 라벨 — 사이트 로고(`~/LeeSeungYong.log`), 푸터 저작권, 사이드바 프로필 이름, meta `author`
- schema.org JSON-LD는 `name: LeeSeungYong` + `alternateName: '이승용'`로 양쪽 유지

**왜:** 사이트 전체를 영문명으로 바꿨더니 "…개발자, LeeSeungYong입니다." 처럼 한글 문장 안에서 어색하게 읽혀 사용자가 되돌려 달라고 함.
