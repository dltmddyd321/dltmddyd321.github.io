---
title: "티켓 링크 하나로 크래시 원인까지 — 사내 Slack 에이전트 만들기"
description: "고객 지원 티켓 링크에 자동으로 크래시 검색 결과를 붙여주는 단순 자동화에서, 실제 크래시 데이터를 읽고 진단까지 써주는 에이전트로 넘어간 과정을 정리했습니다."
pubDate: 2026-09-22T06:30:00Z
category: ai-lab
tags: ["claude-code", "slack", "zendesk", "firebase", "automation"]
aiPreview: "고객 지원 티켓 링크가 올라올 때마다 사용자 ID를 복사해 크래시 리포팅 콘솔에서 검색하는 반복 업무를, Slack Link Unfurl 기능으로 자동화했습니다. 여기까지는 정규식과 REST API 호출뿐인 순수 자동화였는데, 한 단계 더 나아가 실제 크래시 데이터를 BigQuery로 읽고 티켓 문의 내용과 대조해 진단 문장을 만드는 에이전트로 확장했습니다. 그 과정에서 필요한 GCP/Slack 권한 조건들과, 판단이 필요한 지점과 필요 없는 지점을 어떻게 나눴는지를 정리했습니다."
---

티켓 링크 하나로 크래시 원인까지 — 사내 Slack 에이전트 만들기

사내에는 "이슈문의" 계열 채널이 몇 개 있다. 고객 지원 티켓 시스템(Zendesk)에서 티켓이 생기면, 담당자가 그 링크를 채널에 공유하면서 도움을 요청한다. 그런데 링크만 봐서는 뭐가 문제인지 알 수가 없다. 실제 원인을 보려면 그 사용자의 크래시 로그를 봐야 하는데, 절차가 매번 똑같다. 티켓을 열어서 사용자 식별자(User ID)를 확인하고 크래시 리포팅 콘솔(Firebase Crashlytics)로 가서 그 값을 검색한다. 별로 어려운 일은 아닌데, 하루에도 몇 번씩 반복하다 보면 그 자체가 업무 시간을 갉아먹는다.

이 반복을 없애기로 했다. 처음엔 그냥 링크에 검색 결과를 붙여주는 봇으로 시작했고 나중엔 실제 크래시 데이터를 읽고 진단까지 써주는 에이전트로 넘어갔다. 그 과정을 정리한다.

## 1단계: API 호출만으로 끝나는 자동화

첫 버전은 AI가 하나도 안 들어간다. Slack의 Link Unfurl 기능을 썼다.

동작은 이렇다. 누군가 채널에 티켓 링크를 붙여넣으면, Slack이 그 도메인이 우리 앱의 unfurl 대상으로 등록돼 있는 걸 보고 `link_shared` 이벤트를 우리 서버로 보낸다. 서버는 그 이벤트에서 티켓 번호를 뽑아 Zendesk API로 티켓을 조회하고 커스텀 필드 중 User ID 값을 꺼낸다. 그 값으로 Crashlytics 콘솔 검색 URL을 문자열로 조립해서 Slack의 `chat.unfurl` API로 원래 메시지 밑에 카드 형태로 붙여준다.

여기까지는 판단이랄 게 없다. 정규식으로 링크를 걸러내고 REST API 두 번 호출하고 템플릿에 값을 끼워 넣는 게 전부다. 대신 빠르고 거의 공짜다.

이 단계에서 미리 알아야 할 조건들이 있다.

- Slack 쪽: 앱에 `links:read`, `links:write` 봇 스코프가 있어야 하고 App Unfurl Domains에 대상 도메인을 등록해야 `link_shared` 이벤트가 온다. 워크스페이스 설정에 따라 앱 설치 자체가 관리자 승인 대상일 수 있다.
- 서버(웹훅 수신처) 쪽: 서버리스 함수(2세대 Cloud Functions 기준)로 배포하려면 그 프로젝트에 결제 계정이 연결돼 있어야 하고 빌드에 필요한 API들(Cloud Build, Cloud Run, Artifact Registry, Eventarc)이 활성화돼 있어야 한다. 그리고 배포하는 계정 본인에게 `iam.serviceAccountUser` 권한이 있어야 한다 — 프로젝트에 넓은 `Editor` 권한이 있어도 이 권한은 별도로 필요하다. 함수가 빌드될 때 대신 쓰는 서비스 계정을 "내가 이 계정을 대신 써도 된다"고 명시적으로 허락받아야 하는 구조라서다.
- 시크릿 관리 쪽: Secret Manager에 값을 등록하는 것과 그 값을 읽는 것은 별개 권한이다. `Editor` 역할은 시크릿을 만들고 관리할 순 있어도 실제 값을 읽는 권한(`secretmanager.versions.access`)은 기본으로 갖고 있지 않다. 배포 시 시크릿을 함수의 환경변수로 주입하려면, 함수가 실행에 쓰는 서비스 계정에 그 시크릿에 대한 접근 권한을 별도로 부여해야 한다.
- Slack 서명 검증은 필수다. 함수는 외부에서 인증 없이 호출 가능한 상태로 열어둬야 하는데(Slack이 GCP 계정으로 인증하는 게 아니라서), 대신 요청에 실린 Slack 서명을 매번 검증해서 신뢰할 수 없는 호출은 아무 작업도 하지 않고 즉시 걸러낸다.

이 조건들만 맞으면 배포 자체는 어렵지 않다. 실제로 걸리는 부분은 대부분 권한 쪽이다.

## 2단계: 에이전트로 넘어가기

링크만 주는 걸로도 충분히 만족스러웠는데, 한 발 더 나가보기로 했다. 링크를 누르고 들어가서 검색하고 목록을 훑어보는 것도 결국 사람이 하는 일이다. 그 대신 크래시 목록을 미리 읽어서 "지금 이런 문제들이 있고, 티켓 문의 내용이랑 가장 관련 있어 보이는 건 이거다"까지 판단해서 알려주면 그 단계도 줄어든다.

이때부터 진짜 AI가 들어간다. 구조는 이렇게 바뀐다.

1. Crashlytics의 크래시 데이터를 BigQuery로 내보내도록 연동한다. 링크만 만들 땐 필요 없었지만 실제 크래시 목록을 코드가 읽으려면 구조화된 데이터가 필요하다.
2. 함수는 이제 1단계처럼 링크만 조립하는 대신, 그 User ID로 최근 크래시 목록(예외 종류, 발생 횟수, 앱 버전)을 BigQuery에서 쿼리한다.
3. 그 목록과 티켓의 문의 내용(자연어)을 LLM에 같이 넘긴다. LLM은 "이 사용자가 최근 겪은 예외들 중 어떤 게 이 문의와 관련 있어 보이는가"를 판단해서 짧은 진단 문장을 만든다.
4. 그 진단을 스레드 답장으로 채널에 남긴다.

여기서 추가로 필요한 조건이 하나 더 생긴다. Crashlytics를 BigQuery로 내보내려면 그 Firebase 프로젝트가 종량제(Blaze) 요금제여야 한다. 무료 요금제(Spark) 프로젝트에서는 애초에 이 연동 자체가 안 뜬다. 이미 결제가 활성화된 프로젝트라면 상관없지만 사이드 프로젝트처럼 결제를 안 걸어둔 프로젝트에서 시도하면 이 지점에서 막힌다.

구조도 하나 바꿔야 했다. 1단계는 이벤트를 받고 몇 초 안에 응답을 끝냈는데, BigQuery 쿼리와 LLM 호출까지 더하면 Slack이 이벤트 처리 완료로 인정해주는 시간(3초 안팎)을 넘길 수 있다. 그래서 두 단계로 쪼갰다. 링크 미리보기는 지금처럼 즉시 붙이고 AI 진단은 비동기로 처리해서 준비되는 대로 스레드에 따로 남기는 식이다. 사용자 입장에선 링크 밑에 카드가 먼저 뜨고 몇 초 뒤에 봇의 분석 답글이 이어서 달리는 것처럼 보인다.

한 가지는 처음부터 정해두고 시작했다. LLM이 내놓는 진단은 항상 "AI 추정"이라고 표시하고 원본 데이터로 확인할 수 있는 링크를 같이 붙인다. 진단이 틀렸을 때 그 말만 믿고 엉뚱한 곳을 고치는 것보다는, 안 믿고 원본을 한 번 더 보는 게 훨씬 낫다.

## 실제로 이렇게 뜬다

채널에 티켓 링크를 붙이면 이런 식으로 흘러간다.

```
#이슈문의-android

김민수  오후 2:14
이거 고객 문의 좀 봐주세요
https://support.example.com/agent/tickets/102384

┌───────────────────────────────────────────┐
│ Crashlytics 검색 · user id 88214             │
│ 로그인 후 홈 화면 진입 시 앱이 자꾸 꺼져요        │
│ console.firebase.google.com                 │
└───────────────────────────────────────────┘

  ↳ 1개의 답글  마지막 답장 1분 전

크래시 분석봇  APP  오후 2:14
이 user id의 최근 7일 크래시 3건을 확인했습니다.

• NullPointerException — HomeActivity.onResume (2건, 6.3.15)
• OutOfMemoryError — CalendarView.render (1건, 6.3.14)

티켓 문의("홈 화면 진입 시 꺼짐")와 가장 관련 있어 보이는 건
HomeActivity.onResume의 NPE입니다. 6.3.15 업데이트 이후 발생.

⚠️ AI 추정입니다 — 정확한 원인은 Crashlytics 원본에서 직접 확인하세요.
→ Firebase Console에서 보기
```

<!-- 스크린샷 위치 제안: 위 텍스트 목업 대신, 실제 Slack 채널에서 이 카드+답글이 뜬 화면을 캡처해서 넣으면 훨씬 생생하다. 회사 워크스페이스/채널명/실제 도메인이 찍히지 않게 크롭하거나 모자이크할 것. -->

첫 카드는 1단계(unfurl)가 만들고, 그 아래 봇 답글은 2단계(에이전트)가 만든다. 같은 파이프라인 안에 결정론적인 부분과 AI가 판단하는 부분이 섞여 있는 셈이다.

## 정리

이걸 "AI로 문제를 해결했다"고 부를 수 있을까. 배포된 시스템만 놓고 보면 반은 맞고 반은 틀리다. 링크를 붙이는 부분은 처음부터 끝까지 AI가 없다. 정규식과 REST API 호출뿐이다. 진짜 판단이 들어가는 건 크래시 목록과 문의 내용을 대조해서 진단을 만드는 마지막 한 단계뿐이다.

그런데 이 나머지를 만드는 과정 자체는 AI 없이는 훨씬 오래 걸렸을 거다. 코드베이스에서 필요한 값들을 찾아내고 권한 문제를 하나씩 짚어가고, 코드를 쓰고 검증하는 것까지 대화 몇 번으로 끝났다. 그러니 "AI가 실시간으로 판단해서 답을 준다"보다는 "반복 업무를 없애는 자동화를, AI 페어프로그래밍으로 빠르게 만들었다" 쪽이 지금 이 시스템을 더 정확하게 설명하는 말이다. 판단이 필요한 지점에는 AI를 넣고 필요 없는 지점(링크 매칭, API 호출, 서명 검증)은 그냥 코드로 남겨두는 것 — 그 경계를 정확히 긋는 게 이번 작업에서 제일 중요했던 부분이다.

<!-- HUMANIZE-SUMMARY v1.6.1
run_id: 2026-09-22-002
mode: monolith (route_hint=light, 강도=보수)
metrics:
  char_in: 4414
  char_out: 4395
  change_rate: 0.6%
  self_check: 6/6
  grade: B
categories:  # before → after
  C-11 연결어미(-고/-지만/-해서) 직후 쉼표: 15 → 0
  F 수식 중복('필요한 관련 API'): 1 → 0
  A-8 이중 피동: 0 → 0
  A-9 '~에 의해' 피동: 0 → 0
  D-1/D-4 결산 lexicon·hype 어휘: 0 → 0
  H-3 메타 진입 '이는~': 0 → 0
self_check:
  - 고유명사·수치·인용 100% 보존: OK (Zendesk/Firebase Crashlytics/BigQuery/Cloud Functions·Build·Run/Artifact Registry/Eventarc/LLM/User ID/unfurl/chat.unfurl/link_shared/Editor/iam.serviceAccountUser/secretmanager.versions.access/Blaze/Spark, 버전 6.3.14·6.3.15, 3초, 7일, 88214, 102384 전부 원형)
  - 변경률 30% 이하: OK (0.6%)
  - 장르 이탈 없음: OK (기술 블로그 유지)
  - register 보존: OK (해라체 평서 그대로, 격식 상향 없음)
  - S1 잔존 0건: OK (C-11 전량 해소, 그 외 S1 원문에 부재)
  - 인공 표현 추가 없음: OK (원문 대시·구어 어미 '거다/안 뜬다/갉아먹는다' 보존, 신규 상투구 0)
highlights:
  - id: C-11
    before: "사용자 식별자(User ID)를 확인하고, 크래시 리포팅 콘솔로 가서"
    after: "사용자 식별자(User ID)를 확인하고 크래시 리포팅 콘솔로 가서"
  - id: C-11
    before: "문자열로 조립해서, Slack의 chat.unfurl API로"
    after: "문자열로 조립해서 Slack의 chat.unfurl API로"
  - id: C-11
    before: "정규식으로 링크를 걸러내고, REST API 두 번 호출하고, 템플릿에"
    after: "정규식으로 링크를 걸러내고 REST API 두 번 호출하고 템플릿에"
  - id: C-11
    before: "링크만 만들 땐 필요 없었지만, 실제 크래시 목록을"
    after: "링크만 만들 땐 필요 없었지만 실제 크래시 목록을"
  - id: F
    before: "빌드에 필요한 관련 API들(Cloud Build, ...)"
    after: "빌드에 필요한 API들(Cloud Build, ...)"
untouched_by_design:
  - 마크다운 구조(헤딩·코드블록·HTML 주석) 무수정, 코드블록 내부 텍스트 전량 원형 보존
  - 조건절 '-면,' 뒤 쉼표(생기면,/붙여넣으면,)는 C-11 대상 어미가 아니어서 보존
  - 대비 '-ㄴ데,'(아닌데,/하는데,/끝냈는데,/만족스러웠는데,)는 자연 구어 리듬으로 보존
  - A-18 관형절 중첩 7건: 재구성 시 의미 드리프트 위험 > 이득, 보수 모드로 미적용
residual_findings:
  - id: A-18
    severity: S2
    reason: "긴 좌향 수식 문장(권한 설명 불릿 등) 잔존 — 분리하면 기술 조건의 인과가 끊길 위험이 커 미적용"
  - id: H-1
    severity: S2
    reason: "문두 접속사(그런데/그리고/그래서/그러니) 5회 — 임계치 경계이나 블로그 구어 voice의 핵심이라 보존"
grade_reason: "B — S1 잔존 0건, 자체검증 6/6 통과. 변경률 0.6%로 A 기준(10~25%) 미달이나, 원문이 이미 light 등급(risk_band medium, 카운트형 피동·어휘 티 0건)이라 보수 강도에서 최소 개입이 정답. 과윤문 회피를 우선함."
-->
